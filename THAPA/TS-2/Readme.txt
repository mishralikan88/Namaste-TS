 // TSconfig features

 // tsc --init to generate tsconfig.json file where we configure typescript code.
 // "noEmitOnError": true,  = >  when tsc compiles the ts file and encounters any error , it does not generate the js file.