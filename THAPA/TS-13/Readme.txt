Type Alias
==========
Type Alias is a way to give a name to a specific type or combination of types .It allows us to create custom name for a type, 
making it easier to reuse and refer to the same type in different parts of the code .Type alias provides better readability,
organisation and abstraction of complex types.

To define a type alias we use the type keyword followed by alias name and the type defination.