Union
=====

Union type allows a variable to have  multiple value types. We use a | (pipe) symbol to define Union.


Intersection
============

Intersection type allows us to combine multiple types into a single type .We use a & (ampersand) symbol to define Intersection.