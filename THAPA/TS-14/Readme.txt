Call signatures in TS
=====================

The functiona call signature refers to the declaration or defination of a function, which includes the function name, parameters and
return type. It defines the structure and type information of a function without including the function's implememntation 
or body

call signatures are typically used inside object type annotation  to describe the shape of the functions with object types.