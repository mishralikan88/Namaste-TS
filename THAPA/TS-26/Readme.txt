Short-Hand properties
=====================

With Shorthand Properties, whenever we have a variable which has the same name as a property on an object,when constructing the object,
we can omit the property name.


